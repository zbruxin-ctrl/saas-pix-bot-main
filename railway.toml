[build]
nixpacksBuildCommand = "npm install && npx prisma generate --schema=./prisma/schema.prisma && rm -rf apps/bot/dist apps/api/dist && npm run build"

[deploy]
startCommand = "sh start.sh"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
